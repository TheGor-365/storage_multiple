<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<%#= image_tag(@post.image) %>

<% (0...@post.images.count).each do |image| %>
  <%= image_tag(@post.thumbnail(image)) %> <br>
<% end %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
